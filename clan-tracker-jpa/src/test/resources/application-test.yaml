spring:
  datasource:
    url: jdbc:tc:postgresql:14.0:///clan-tracker
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: validate
    database: postgresql
